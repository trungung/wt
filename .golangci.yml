version: "2"

linters:
  settings:
    errcheck:
      check-blank: false
      check-type-assertions: false
      exclude-functions:
        - (io.Closer).Close
        - (*github.com/gofrs/flock.Flock).Unlock
